# Site generation notes

The site was generated from `assets/source/NOXIVITRES_SPECIES_BIBLE.md`. The split pages are committed intentionally for GitHub Pages compatibility and fast navigation. Use `verify_integrity.py` to ensure the source and generated page counts still match the shipped build.
